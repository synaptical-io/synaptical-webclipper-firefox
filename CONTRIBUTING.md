## Commit messages

This repo uses [Conventional Commits](https://www.conventionalcommits.org).

Format:

```text
<type>(<scope>): <short summary>
```

Examples:
* feat(background): add native messaging bridge
* fix(mini-panel): handle offscreen positioning
* docs(readme): describe dev environment setup
